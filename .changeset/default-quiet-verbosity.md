---
'kimaki': minor
---

Default new channels to text-only output so tool calls and status messages no longer flood Discord. Kimaki still refreshes Discord's typing indicator while the agent is actively working, and `/verbosity` can opt a channel into tool output.
